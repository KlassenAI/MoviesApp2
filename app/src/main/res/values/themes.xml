<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MoviesApp" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primaryLight</item>
        <item name="colorPrimaryVariant">@color/primaryVariantLight</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/secondaryLight</item>
        <item name="colorSecondaryVariant">@color/secondaryVariantLight</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="themeMessage" parent="message">
        <item name="drawableTint">@color/black</item>
    </style>

    <style name="themeEditText" parent="editText">
        <item name="android:background">@color/white</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="themeToolbar" parent="toolbar">
        <item name="android:background">@color/primaryLight</item>
    </style>

    <style name="themeButton" parent="button">
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="themePrimaryText" parent="primaryText">
        <item name="android:textColor">@color/primaryTextLight</item>
    </style>

    <style name="themeSecondaryText" parent="secondaryText">
        <item name="android:textColor">@color/secondaryTextLight</item>
    </style>

    <style name="themeBottomNavigation" parent="Widget.MaterialComponents.BottomNavigationView.Colored">
        <item name="itemTextColor">@drawable/bottom_nav_color_light</item>
        <item name="itemIconTint">@drawable/bottom_nav_color_light</item>
    </style>

    <!-- Customize your styles here. -->

    <color name="themeGenreStroke">@color/black</color>
    <color name="themeGenreBackground">@color/white</color>


    <style name="btnLike">
        <item name="android:layout_height">32dp</item>
        <item name="android:layout_marginEnd">8dp</item>
        <item name="android:layout_marginBottom">8dp</item>
        <item name="android:contentDescription">@string/cd_like</item>
        <item name="android:tint">@color/black</item>
    </style>



    <style name="formEditText">
        <item name="android:layout_margin">8dp</item>
        <item name="android:background">@drawable/round_form</item>
    </style>

    <style name="imageEditText">
        <item name="android:layout_marginStart">8dp</item>
        <item name="android:tint">@color/black</item>
    </style>

    <style name="btn_filter">
        <item name="android:backgroundTint">@color/white</item>
        <item name="android:insetTop">0dp</item>
        <item name="android:insetBottom">0dp</item>
        <item name="android:padding">4dp</item>
        <item name="android:layout_marginTop">8dp</item>
        <item name="android:layout_marginEnd">8dp</item>
        <item name="android:layout_marginBottom">8dp</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">16sp</item>
        <item name="android:contentDescription">@string/cd_search_filter</item>
    </style>

</resources>